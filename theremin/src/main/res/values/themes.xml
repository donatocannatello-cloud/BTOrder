<resources>
    <!-- Tema base scuro: la UI è interamente realizzata con Jetpack Compose -->
    <style name="Theme.Theremin" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
